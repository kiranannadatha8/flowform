# FormFlow — context for Claude / Cursor

**FormFlow** is a Next.js 16 app: Zod `FormDefinition`, Prisma + Postgres, builder (`/builder`), published runner (`/f/[slug]`), public REST under `/api/public/forms/`, optional AI in `src/lib/formflow/ai/`.

Start with **`README.md`** for setup, environment variables, and the full API/route list (includes **Phase 3** responses + optional builder password). Agent-specific Next.js notes: **`AGENTS.md`**.
